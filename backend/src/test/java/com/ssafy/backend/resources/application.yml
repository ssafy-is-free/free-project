spring:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl_auto: create-drop
    database: h2
    generate-ddl: off
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;MODE=MySQL;
    username: SA
    password: