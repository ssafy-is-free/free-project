/**
 * onClick : 모달창 닫는 메소드
 */
export interface IBojProps {
  onClick: () => void;
}

/**
 * 1. onClick : 모달창 닫는 메소드
 * 2. setOpenBoj:  백준 모달 여는 setState
 */
export interface ILoginProps {
  onClick: () => void;
  // setOpenBoj: React.Dispatch<React.SetStateAction<boolean>>;
}
