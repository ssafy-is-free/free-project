version: "3"
services:
  backend: 
    images: sh80165/product-free-springboot
    container_name: product_be
    restart: always
    ports: 
     - 8080:8080
    networks:
      - app_network
  frontend: 
    images: sh80165/product-free-react
    container_name: product_fe
    restart: always
    ports: 
     - 3000:3000
    networks:
      - app_network
  data: 
    images: sh80165/product-free-data
    container_name: product_data
    restart: always
    ports: 
     - 8000:8000
    networks:
      - app_network
networks:
  app_network: 
    external: true

